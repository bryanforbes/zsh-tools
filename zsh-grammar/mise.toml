[tasks.extract-raw-grammar]
depends = ['//vendor:prepare']
sources = ['//vendor/zsh/Src/*.c', '//vendor/zsh/Src/zsh.h', 'src/zsh_grammar/extract_zsh_raw.py']
outputs = ['zsh_raw_extraction.json']
description = "Extract raw Zsh grammar"
run = 'uv run extract_zsh_raw'
